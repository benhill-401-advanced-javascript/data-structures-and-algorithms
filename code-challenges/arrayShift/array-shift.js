'use strict';

let array = [1, 2, 3, 4, 5];
array[array.length] = 6;
function insertShiftArr(list) {
  let middle = list.length / 2;
} for (array) {
  let someting = list[array];

}

// add six to the end of the array
// loop through the length of the array and divide it's length by two after we've added the 6